# 上校工具箱 (Colonel's Toolbox)

一个基于 Go (Gin) 和 Vue 3 的轻量级、模块化开发者工具箱。

## 🛠️ 功能列表
1.  **JSON 工具**: 格式化、压缩、语法高亮 (左右分栏布局)。
2.  **Base64 / 加解密**: Base64 编解码、AES/DES/RSA 加解密。
3.  **正则测试**: 实时正则表达式匹配与高亮。
4.  **Cron 表达式**: Crond 表达式生成与反解析。
5.  **时间戳工具**: 时间戳转换 (支持秒/毫秒)。
6.  **URL 编解码**: URL Encode/Decode。
7.  **UUID 生成器**: 批量生成 UUID (v1, v4)。
8.  **哈希计算**: MD5, SHA1, SHA256, SHA512 计算。
9.  **工具调用统计**: 查看工具使用热度与趋势。

> 🛡️ **安全增强**: API 接口已启用 IP 速率限制 (Rate Limiting)，防止恶意刷量。

## 在线体验地址：
https://tools.sonic.nyc.mn/


## 🚀 快速开始

### 本地运行
1.  **构建前端**:
    ```bash
    cd frontend
    npm install
    npm run build
    ```
2.  **运行后端**:
    ```bash
    go run main.go
    # 或构建二进制
    go build -o toolbox.exe
    ./toolbox.exe
    ```
3.  访问 `http://localhost:8080`

### 🐳 Docker 部署 (推荐)
本项目已完全容器化，且已推送至 DockerHub。

#### 方式一：直接运行 (推荐)
```bash
docker run -d \
  --name my-tools \
  -p 8080:8080 \
  -v tools_data:/data \
  isolatec/tools:latest
```

#### 方式二：手动构建
```bash
# 自动使用 goproxy.cn 加速
docker build -t tools:latest .
```

#### 挂载说明
*   `-v tools_data:/data`: **[重要]** SQLite 数据库存储在容器内的 `/data` 目录，挂载卷以确保统计数据持久化。

